---
import Layout from '../layouts/Layout.astro';
---

<Layout 
  title="TypaVibe - Create collections in 30 seconds"
  description="Create and share curated product collections instantly. No signup needed. Start curating your vibe in 30 seconds."
  image="/og-image.png"
  url={Astro.url.href}
  type="website"
>
  <main>
    <section class="hero">
      <h1>Curate anything, share everything</h1>
      <p class="hero-subtitle">The simplest way to create and share collections of anything you love</p>
      
      <!-- Single CTA button -->
      <button id="start-collection-btn" class="btn-primary hero-cta">
        🚀 Start Your Collection
      </button>
    </section>
    
    <!-- Use cases -->
    <section class="use-cases">
      <h2>Perfect for anyone who loves to share</h2>
      <div class="use-cases-grid">
        <div class="use-case">
          <div class="use-case-icon">🛍️</div>
          <h3>Shoppers</h3>
          <p>Share your favorite finds, wishlists, and shopping recommendations</p>
        </div>
        <div class="use-case">
          <div class="use-case-icon">📚</div>
          <h3>Learners</h3>
          <p>Curate resources, tools, and learning materials for your community</p>
        </div>
        <div class="use-case">
          <div class="use-case-icon">🎨</div>
          <h3>Creators</h3>
          <p>Showcase inspiration, tools, and resources that fuel your creativity</p>
        </div>
        <div class="use-case">
          <div class="use-case-icon">💼</div>
          <h3>Professionals</h3>
          <p>Share industry insights, tools, and resources with your network</p>
        </div>
        <div class="use-case">
          <div class="use-case-icon">🏠</div>
          <h3>Homeowners</h3>
          <p>Create home improvement guides, decor ideas, and renovation resources</p>
        </div>
        <div class="use-case">
          <div class="use-case-icon">🎯</div>
          <h3>Enthusiasts</h3>
          <p>Build communities around your passions, hobbies, and interests</p>
        </div>
      </div>
    </section>
    
    <!-- Features -->
    <section class="features">
      <h2>Why TypaVibe?</h2>
      <div class="features-grid">
        <div class="feature">
          <h3>⚡ Instant</h3>
          <p>Create and share in 30 seconds. No account needed.</p>
        </div>
        <div class="feature">
          <h3>🔗 Simple</h3>
          <p>Just add links. We handle the rest automatically.</p>
        </div>
        <div class="feature">
          <h3>📱 Shareable</h3>
          <p>One link to share your entire collection anywhere.</p>
        </div>
        <div class="feature">
          <h3>🎨 Beautiful</h3>
          <p>Clean, modern design that makes your content shine.</p>
        </div>
      </div>
    </section>
    
    <!-- Navigation links -->
    <section style="text-align: center; margin-top: 48px;">
      <div style="display: flex; gap: 16px; justify-content: center; flex-wrap: wrap;">
        <a href="/explore" class="btn-secondary">Explore Collections</a>
        <a href="/auth" class="btn-secondary">Sign In</a>
      </div>
    </section>
  </main>
  
  <!-- Auth Choice Modal -->
  <div id="auth-modal" class="modal" style="display: none;">
    <div class="modal-content">
      <div class="modal-header">
        <h2>How would you like to start?</h2>
        <button class="modal-close" onclick="closeAuthModal()">×</button>
      </div>
      
      <div class="auth-options">
        <!-- Anonymous Option -->
        <div class="auth-option-card">
          <div class="option-icon">🚀</div>
          <h3>Start Anonymously</h3>
          <p>Create your collection right now. Save locally and share with a link.</p>
          <div class="option-benefits">
            <span>✅ No signup required</span>
            <span>✅ Start immediately</span>
            <span>✅ Share with link</span>
          </div>
          <button onclick="startAnonymous()" class="btn-primary">Start Now</button>
        </div>
        
        <!-- Account Option -->
        <div class="auth-option-card">
          <div class="option-icon">👤</div>
          <h3>Create Account</h3>
          <p>Sign up or sign in to save collections permanently and access them from anywhere.</p>
          <div class="option-benefits">
            <span>✅ Permanent storage</span>
            <span>✅ Share with username</span>
            <span>✅ Access from anywhere</span>
          </div>
          <button onclick="window.location.href='/auth'" class="btn-secondary">Sign In / Sign Up</button>
        </div>
      </div>
    </div>
  </div>
</Layout>

<script>
  import { supabase } from '../lib/supabase.js';
  
  let currentUser = null;
  
  // Check authentication status
  async function checkAuth() {
    try {
      const { data: { user }, error } = await supabase.auth.getUser();
      if (!error && user) {
        currentUser = user;
        console.log('User authenticated:', user.email);
      }
    } catch (error) {
      console.error('Auth check error:', error);
    }
  }
  
  // Handle start collection button
  document.getElementById('start-collection-btn').addEventListener('click', async () => {
    if (currentUser) {
      // User is logged in, go directly to create
      window.location.href = '/create';
    } else {
      // User is not logged in, show auth modal
      showAuthModal();
    }
  });
  
  function showAuthModal() {
    document.getElementById('auth-modal').style.display = 'flex';
    document.body.style.overflow = 'hidden';
  }
  
  function closeAuthModal() {
    document.getElementById('auth-modal').style.display = 'none';
    document.body.style.overflow = 'auto';
  }
  
  function startAnonymous() {
    console.log('startAnonymous called');
    closeAuthModal();
    window.location.href = '/create';
  }
  
  // Close modal when clicking outside
  document.getElementById('auth-modal').addEventListener('click', (e) => {
    if (e.target.id === 'auth-modal') {
      closeAuthModal();
    }
  });
  
  // Make functions globally accessible
  window.startAnonymous = startAnonymous;
  window.showAuthModal = showAuthModal;
  window.closeAuthModal = closeAuthModal;
  
  // Initialize
  checkAuth();
</script>

<style>
  .hero-cta {
    font-size: 18px;
    padding: 16px 32px;
    font-weight: 600;
  }
  
  .hero-subtitle {
    font-size: 18px;
    color: var(--color-text-secondary);
    margin-bottom: 32px;
    line-height: 1.5;
  }
  
  /* Modal Styles */
  .modal {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.5);
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 1000;
  }
  
  .modal-content {
    background: var(--color-background);
    border-radius: var(--radius);
    padding: 32px;
    max-width: 600px;
    width: 90%;
    max-height: 90vh;
    overflow-y: auto;
    box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
  }
  
  .modal-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 24px;
  }
  
  .modal-header h2 {
    margin: 0;
    font-size: 24px;
    font-weight: 600;
  }
  
  .modal-close {
    background: none;
    border: none;
    font-size: 24px;
    cursor: pointer;
    color: var(--color-text-secondary);
    padding: 0;
    width: 32px;
    height: 32px;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 50%;
    transition: all 0.2s;
  }
  
  .modal-close:hover {
    background: var(--color-background-secondary);
    color: var(--color-text);
  }
  
  .auth-options {
    display: grid;
    gap: 20px;
    grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
  }
  
  .auth-option-card {
    background: var(--color-background-secondary);
    border: 1.5px solid var(--color-border);
    border-radius: var(--radius);
    padding: 24px;
    text-align: center;
    transition: all 0.2s ease;
  }
  
  .auth-option-card:hover {
    border-color: var(--color-accent);
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
  }
  
  .option-icon {
    font-size: 32px;
    margin-bottom: 16px;
  }
  
  .auth-option-card h3 {
    margin: 0 0 12px 0;
    font-size: 18px;
    font-weight: 600;
    color: var(--color-text);
  }
  
  .auth-option-card p {
    margin: 0 0 16px 0;
    color: var(--color-text-secondary);
    line-height: 1.5;
    font-size: 14px;
  }
  
  .option-benefits {
    display: flex;
    flex-direction: column;
    gap: 8px;
    margin-bottom: 20px;
    text-align: left;
  }
  
  .option-benefits span {
    font-size: 13px;
    color: var(--color-text-secondary);
  }
  
  .auth-option-card .btn-primary,
  .auth-option-card .btn-secondary {
    width: 100%;
    margin-top: 8px;
  }
  
  .use-cases {
    margin-top: 80px;
    text-align: center;
  }
  
  .use-cases h2 {
    margin-bottom: 48px;
    font-size: 28px;
    font-weight: 600;
    color: var(--color-text);
  }
  
  .use-cases-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 24px;
    max-width: 1200px;
    margin: 0 auto;
  }
  
  .use-case {
    background: var(--color-background-secondary);
    border: 1.5px solid var(--color-border);
    border-radius: var(--radius);
    padding: 24px;
    text-align: center;
    transition: all 0.2s ease;
  }
  
  .use-case:hover {
    border-color: var(--color-accent);
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
  }
  
  .use-case-icon {
    font-size: 32px;
    margin-bottom: 16px;
  }
  
  .use-case h3 {
    margin: 0 0 12px 0;
    font-size: 18px;
    font-weight: 600;
    color: var(--color-text);
  }
  
  .use-case p {
    margin: 0;
    color: var(--color-text-secondary);
    line-height: 1.5;
    font-size: 14px;
  }
  
  .features {
    margin-top: 80px;
    text-align: center;
  }
  
  .features h2 {
    margin-bottom: 48px;
    font-size: 28px;
    font-weight: 600;
    color: var(--color-text);
  }
  
  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 24px;
    max-width: 1000px;
    margin: 0 auto;
  }
  
  .feature {
    padding: 24px;
    text-align: center;
  }
  
  .feature h3 {
    margin: 0 0 12px 0;
    font-size: 18px;
    font-weight: 600;
    color: var(--color-text);
  }
  
  .feature p {
    margin: 0;
    color: var(--color-text-secondary);
    line-height: 1.5;
    font-size: 14px;
  }
  
  @media (max-width: 768px) {
    .auth-options {
      grid-template-columns: 1fr;
    }
    
    .use-cases-grid {
      grid-template-columns: 1fr;
    }
    
    .features-grid {
      grid-template-columns: 1fr;
    }
    
    .use-cases h2,
    .features h2 {
      font-size: 24px;
    }
    
    .modal-content {
      padding: 24px;
      margin: 16px;
    }
  }
</style>
